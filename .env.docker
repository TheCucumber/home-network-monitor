# Docker Environment Configuration Example
# Copy this file to .env and customize as needed

# Server Configuration
PORT=8000
HOST=0.0.0.0

# Ping Configuration
PING_INTERVAL=60          # Ping interval in seconds
PING_TIMEOUT=5            # Timeout for each ping in seconds
PING_RETRIES=2            # Number of retry attempts

# Database Configuration
DATABASE_PATH=/app/data/pings.db

# Data Retention
RETENTION_DAYS=120        # Days to keep ping data
CLEANUP_HOUR=2            # Hour of day to run cleanup (24h format)

# Monitored Hosts (comma-separated)
# Add your hosts here (e.g., router IP, DNS servers, important services)
MONITORED_HOSTS=router.local,8.8.8.8,1.1.1.1,192.168.1.1

# CORS Origins (comma-separated)
# Allow requests from these origins
CORS_ORIGINS=http://localhost,http://localhost:80,http://localhost:3000
